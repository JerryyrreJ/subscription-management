# Development Notes

This folder contains historical development notes, implementation summaries, design drafts, and older setup drafts.

These files are kept for project memory. They are not the public documentation surface. Public documentation lives in `docs/`.

## Archive

The `archive/` folder contains the documents that used to live in the root directory or the public `docs/` folder.
